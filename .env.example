# 站点访问密码，打开站点的第一道锁。必填，缺失则启动失败
PASSWORD=change-me-please

# 会话 Cookie 的 HMAC 密钥，至少 32 字节。生成：openssl rand -hex 32
SESSION_SECRET=replace-with-at-least-32-bytes-random-string

# 可选：监听端口，默认 3000
PORT=3000

# 可选：数据目录，默认 ./data。容器内为 /app/data
DATA_DIR=./data
